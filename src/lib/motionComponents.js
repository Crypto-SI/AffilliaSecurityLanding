'use client';

// Import specific components from framer-motion
import {
  motion,
  AnimatePresence,
  useAnimationControls,
  useMotionValue,
  useTransform,
  useReducedMotion,
  useScroll
} from 'framer-motion';

// Re-export the specific components
export {
  motion,
  AnimatePresence,
  useAnimationControls,
  useMotionValue,
  useTransform,
  useReducedMotion,
  useScroll
}; 